import * as express from "express";
declare const router: import("express-serve-static-core").Router;
export declare const health: (_: any, res: express.Response) => void;
export declare const version: (_: any, res: express.Response) => void;
export default router;
